to setup the env:  
`pip install pipenv`  
`$pipenv install --dev`

to install new packages:  
`$pipenv install ... # some package`

to run tests:  
`$pipenv run pytest`  
or  
`$pipenv shell`  
`$pytest`  

to enter the shell:  
`pipenv shell`  

to leave the shell:
`exit`  